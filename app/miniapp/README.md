# Minimal example of Admin NextGraph's SDK

Components:

1. rust-sdk
2. js-sdk wth lib-wasm
3. a rust/wasm worker
4. a typescript app using svelte

The app lets a user connect to a given remote broker with a given wallet,
and then list all users on that broker and display their usage statistics.
